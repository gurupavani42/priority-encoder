# priority-encoder
**Priority Encoder:** A priority encoder is a digital circuit that gives **priority to the highest-priority active input** and converts it into a binary code. It is used when **more than one input is active at the same time**.
